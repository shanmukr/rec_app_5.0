<tr id="hide">
m	
  <td>
	  <%= form_tag(resume_status_to_rejected_resumes_path, :onsubmit => "auto_hide_the_fields(event, \'#{@resume\'},'Rejected');", remote: true) do %>
      <%= text_area_tag "comment",nil, :id => @resume %>
			<%= hidden_field_tag "id", @resume %>
			<%= hidden_field_tag "type", "USER" %>
			<%= hidden_field_tag "status", "REJECTED" %>
			<%= submit_tag "Go" %>
		<% end %>
  </td>
</tr>
